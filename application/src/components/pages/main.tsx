import React, { Component } from "react";

export default class MainPage extends Component {
  render(): React.ReactNode {
    return (
      <>
        <h2 className="header-page">MainPage</h2>
      </>
    );
  }
}
